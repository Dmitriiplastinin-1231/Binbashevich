# Система обнаружения и блокировки подозрительного сетевого трафика

## 🛡️ Описание

Система для мониторинга, обнаружения и блокировки подозрительного сетевого трафика с использованием Python и Scapy. Включает графический интерфейс для удобного управления и мониторинга.

## ✨ Возможности

- **Мониторинг в реальном времени** - захват и анализ сетевых пакетов
- **Обнаружение угроз:**
  - Сканирование портов
  - SYN flood атаки
  - ICMP flood атаки
  - UDP flood атаки
  - Аномально большие пакеты
  - Превышение лимита соединений
- **Автоматическая блокировка** - через iptables (симуляция)
- **ICMP Unreachable** - отправка сообщений о недостижимости
- **Графический интерфейс** - интуитивный и информативный
- **Логирование** - подробные логи всех событий
- **Статистика** - в реальном времени

## 📦 Установка

### 1. Установка зависимостей

```bash
cd /workspaces/Binbashevich/gnu/ip
pip install -r requirements.txt
```

### 2. Установка Scapy (если нужно)

```bash
pip install scapy
```

### 3. Права суперпользователя

Для захвата пакетов и работы с iptables нужны права root:

```bash
sudo -i
```

## 🚀 Запуск

### Графический интерфейс

```bash
sudo python gui.py
```

### Только мониторинг (без GUI)

```python
from traffic_analyzer import TrafficAnalyzer
from firewall_manager import FirewallManager
from traffic_monitor import TrafficMonitor

analyzer = TrafficAnalyzer()
firewall = FirewallManager()
monitor = TrafficMonitor(analyzer, firewall)

monitor.start_monitoring()
```

## 🎯 Использование

### Через GUI:

1. **Запустите приложение** - `sudo python gui.py`
2. **Укажите интерфейс** - `any` для всех интерфейсов или конкретный (eth0, wlan0)
3. **Опционально**: добавьте BPF фильтр (например, `tcp port 80`)
4. **Нажмите "Запустить мониторинг"**
5. **Наблюдайте** за событиями и статистикой
6. **Управляйте блокировками** через панель заблокированных IP

### Правила обнаружения:

```python
# Настройки в traffic_analyzer.py
MAX_PACKET_SIZE = 10000  # байт
PORT_SCAN_THRESHOLD = 10  # портов
CONNECTION_THRESHOLD = 50  # соединений в минуту
```

## 📊 Структура проекта

```
gnu/ip/
├── gui.py                  # Графический интерфейс
├── traffic_analyzer.py     # Анализатор трафика
├── traffic_monitor.py      # Мониторинг в реальном времени
├── firewall_manager.py     # Управление файрволом
├── requirements.txt        # Зависимости
└── README.md              # Документация
```

## 🔧 Модули

### TrafficAnalyzer
Анализирует пакеты и определяет подозрительную активность:
- Проверка размера пакетов
- Обнаружение сканирования портов
- Обнаружение flood-атак
- Подсчет соединений

### FirewallManager
Управляет блокировкой IP-адресов:
- Добавление правил iptables
- Удаление правил
- Отправка ICMP сообщений
- Ведение списка блокировок

### TrafficMonitor
Захватывает и обрабатывает пакеты:
- Использует Scapy для захвата
- Работает в отдельном потоке
- Вызывает callback для GUI
- Собирает статистику

## ⚠️ Важно

### Безопасность:
- Требуются права root для захвата пакетов
- Блокировка через iptables в режиме симуляции по умолчанию
- Для реальной блокировки раскомментируйте код в `firewall_manager.py`

### Производительность:
- При большом трафике может потреблять ресурсы
- Настройте фильтры BPF для оптимизации
- Используйте лимиты в правилах обнаружения

## 🧪 Тестирование

### Генерация тестового трафика:

```python
from scapy.all import *

# SYN flood
for i in range(50):
    send(IP(dst="127.0.0.1")/TCP(dport=80, flags="S"))

# Port scan
for port in range(1, 100):
    send(IP(dst="127.0.0.1")/TCP(dport=port, flags="S"))

# ICMP flood
for i in range(50):
    send(IP(dst="127.0.0.1")/ICMP())
```

## 📝 Логи

Логи отображаются в GUI и включают:
- Системные события (запуск, остановка)
- Обнаруженные угрозы
- Действия по блокировке
- Ошибки и предупреждения

## 🔒 Лицензия

Проект создан в учебных целях.

## 👨‍💻 Разработка

Для расширения функциональности:
1. Добавьте новые правила в `TrafficAnalyzer`
2. Реализуйте дополнительные методы блокировки в `FirewallManager`
3. Улучшите GUI в `gui.py`
4. Добавьте машинное обучение для обнаружения аномалий

## 📧 Поддержка

При возникновении проблем проверьте:
- Права доступа (root)
- Установлены ли зависимости
- Корректность сетевого интерфейса
- Логи в GUI или консоли
